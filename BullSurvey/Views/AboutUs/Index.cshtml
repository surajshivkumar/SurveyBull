@model AboutUsViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>About Us - @Model.WebsiteName</title>
</head>
<body class="bg-gray-50">
    <div class="container mx-auto px-4 py-8">
        <!-- Header -->
        <div class="mb-8">
            <h1 class="text-3xl font-bold mb-8 text-green-500 mb-2 text-center">About Us</h1>
            <p class="text-center text-gray-600">@Model.Description</p>
        </div>

        <!-- Team Members Flexbox Layout -->
        <div class="flex gap-6 justify-center">
            @foreach (var member in Model.TeamMembers)
            {
                <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition-shadow duration-300 text-center w-full sm:w-1/3 lg:w-1/4">
                    <div class="rounded-full overflow-hidden mx-auto mb-4">
                        <img src="@member.PhotoUrl" alt="@member.Name's photo" class="w-full h-full object-cover" />
                    </div>
                    <h3 class="text-xl font-bold text-green-500">@member.Name</h3>
                    <p class="text-gray-600">@member.Role</p>
                    <p class="mt-4 text-gray-700">@member.Bio</p>
                </div>
            }
        </div>
    </div>
</body>
</html>
